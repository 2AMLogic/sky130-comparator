{
  "schema_version": 1,
  "block": "sky130-comparator",
  "kind": "analog",
  "top_cell": {
    "name": "comparator",
    "source": "design/comparator.sch",
    "note": "xschem top-level schematic; netlisted FLAT (no .subckt wrapper) by design/netlist.sh"
  },
  "ports": [
    {
      "name": "VDD",
      "direction": "power",
      "type": "supply",
      "note": "1.8 V core supply (nfet_01v8/pfet_01v8 flavour)"
    },
    {
      "name": "GND",
      "direction": "power",
      "type": "supply",
      "note": "ground"
    },
    {
      "name": "CLK",
      "direction": "input",
      "type": "clock",
      "note": "not a bare StrongARM clock port: the DR-003 soft-clock shaper (R_CLKS x M_CLKCAP, tau ~ 250 ps) forms internal node CLKT driving all five clocked gates, so the evaluate onset is slew-limited by that RC rather than by the external ramp rate"
    },
    {
      "name": "VINP",
      "direction": "input",
      "type": "analog",
      "note": "differential input, true side; Vindiff = VINP - VINN > 0 settles OUTP high"
    },
    {
      "name": "VINN",
      "direction": "input",
      "type": "analog",
      "note": "differential input, complement side"
    },
    {
      "name": "OUTP",
      "direction": "output",
      "type": "digital",
      "note": "full-swing latch output, true side; precharged to VDD during reset (CLK low)"
    },
    {
      "name": "OUTN",
      "direction": "output",
      "type": "digital",
      "note": "full-swing latch output, complement side; precharged to VDD during reset (CLK low)"
    }
  ],
  "netlist": {
    "path": "sim/comparator-decision/testbench/comparator_core.spice",
    "regeneration_command": "./design/netlist.sh",
    "staleness_check": "./design/netlist.sh --check",
    "note": "generated file, never hand-edited; source of truth is design/comparator.sch"
  },
  "gds": {
    "path": "layout/comparator.gds",
    "note": "generated by layout/gen_comparator.py (klt 0.6.0 on klayout 0.30.10, sky130A @ open_pdks c6d73a35f5); top cell gen_compose_0; regenerates byte-for-byte via gen_comparator.py --check -- unchanged across the klt 0.5.0 -> 0.6.0 bump (issue #47). T1 item 2 (issue #44)"
  },
  "measured_area": {
    "value_um2": 3330.0,
    "note": "composed bbox 40.59 x 82.04 um from the gen-compose report (layout/compose-report.json), issue #44"
  },
  "maturity_rung": "pre-T1",
  "maturity_rung_basis": "manifests/t1-signoff-report.json is the graded verdict of record for T1 item status (see the report's t1_met_count/t1_item_count and per-item status; tier null is the honest pre-T1 state per issue #31); rung vocabulary per manifests/design-evidence-tiers.md (T1/bronze = sim-validated, T2/silver, T3/gold)",
  "provenance": {
    "generated_from": [
      "design/comparator.sch",
      "manifests/t1-signoff-report.json"
    ],
    "refresh_rule": "hand-maintained view (issue #36). Refresh: re-derive the port list from design/comparator.sch, re-check maturity_rung against manifests/t1-signoff-report.json, re-read 2am repos.yml consumes: entries for spec/consumers.md, and flip gds/measured_area from null when layout lands. scripts/check-integrator-view.py gate (CI) fails on a missing key, a dangling path, a rung that disagrees with the signoff report, or a maturity_rung_basis that misnames the report or states a T1-items-met count the report does not carry (issue #50).",
    "generated": "2026-09-22, issue #36, against origin/main e084b55"
  }
}
