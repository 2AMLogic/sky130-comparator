{
  "_comment": [
    "Pinned toolchain for this repo's evidence, checked (not merely recorded)",
    "before any PVT point is simulated. Keep in sync with the version table in",
    "docs/environment-setup.md (once that document exists in this repo) --",
    "changing a pin here means re-validating that document and re-running",
    "sim/selftest.sh.",
    "  open_pdks          exact open_pdks commit hash the sky130 models must",
    "                     come from (must match sim/pdk.json's",
    "                     open_pdks_commit). Exact, because the hash IS the",
    "                     device models: a different one silently yields",
    "                     numbers no other record is comparable with. Set to",
    "                     null to disable the check (not recommended).",
    "  ngspice_min_major  floor, not an exact match -- newer ngspice is",
    "                     allowed and recorded; older is refused.",
    "  ngspice_reference  the ngspice major version this repo's harness was",
    "                     bootstrapped and verified against (issue #8). Not a",
    "                     check -- the floor above is the check.",
    "  python_min         floor, as 'major.minor'.",
    "  xschem_tag         upstream release tag the netlisting toolchain is",
    "                     built from.",
    "A drift is fatal by default; --allow-toolchain-drift overrides it and",
    "stamps the drift into every record written under it.",
    "",
    "Provenance: schema ported (issue #8) from 2AMLogic/sky130-sar-adc's",
    "sim/toolchain.json (commit b80c144efbf467a586f728726cabb25c1eb5a1f2),",
    "which itself ports the schema from 2AMLogic/gf180-sar-adc's",
    "sim/toolchain.json (commit f613571aee5b80eff1eea37bdce9dfc88c5cf396),",
    "per CLAUDE.md's 'Harness bootstrap' instruction -- values re-pinned",
    "against this repo's own bootstrap environment, not copied verbatim.",
    "ngspice_min_major is kept at the same floor (46) sky130-sar-adc uses",
    "since this repo verified sim/selftest.sh end to end (issue #8) on",
    "ngspice-46 against the identical open_pdks pin below."
  ],
  "open_pdks": "c6d73a35f524070e85faff4a6a9eef49553ebc2b",
  "ngspice_min_major": 46,
  "ngspice_reference": 46,
  "python_min": "3.9",
  "xschem_tag": "3.4.7"
}
