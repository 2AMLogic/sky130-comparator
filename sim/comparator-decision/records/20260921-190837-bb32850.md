# Record 20260921-190837-bb32850

- **Record ID**: 20260921-190837-bb32850
- **Claim**: None by itself -- top-level README target-spec rows are graded by their ratifying decision record, and per DR-002 (2026-09-16, merged) the Offset sigma, Input-referred noise, and Kickback rows are RATIFIED while Decision time vs. overdrive and Supply/power stay DRAFT/OPEN. What this record characterizes is **this repo's own comparator**: `design/comparator.sch`, the DR-001 topology plus the DR-003 soft-clock shaper on the clock port, at a sizing derived from this PDK's own mismatch models (see the schematic's sizing-rationale block, DR-001 Amendment 1, and DR-003). Any statement about a ratified row's compliance made below cites the bound and the number side by side.
- **Netlist provenance**: schematic-derived, reduced sub-model -- the tail + input pair + DI-node reset PMOS device lines are taken verbatim from `sim/comparator-decision/testbench/comparator_core.spice` (itself generated from `design/comparator.sch` by `./design/netlist.sh`), with the DI-node PMOS re-emitted diode-connected and the tail's DR-003 soft-clock gate node (CLKT) held at the same steady VDD evaluate bias; see Methodology for what the loop break omits.
- **Corner matrix run**: process=['tt'], temperature_c=[27.0], supply_v=[1.8] (1 point)
- **Noise methodology**: `ac-based`, integration bandwidth 1000Hz-1e+09Hz. REDUCED SUB-MODEL, not the full comparator_core.spice fragment: reduced sub-model of the INTEGRATION phase: tail + input pair, with the DI-node precharge PMOS pair diode-connected (self-biased) as the loads on the input pair's own drain nodes DIP/DIN, and the cross-coupled latch pairs omitted because they are off (Vgs ~ 0) until regeneration begins; CLK held at VDD (steady evaluate bias, tail on); noise taken at v(DIP,DIN). This is a named, flagged simplification (excludes the cross-coupled latch pair's own regenerative-phase noise contribution) -- a LOWER BOUND on the true regeneration-inclusive noise, per the port source's own documented derivation (see README.md).
- **Note**: Post-mitigation regression re-run after the DR-003 soft-clock shaper (issue #30; spec/decision-records/DR-003-kickback-slew-limited-clock.md), like-for-like with DR-002 Decision 2 evidence (record 20260916-001416-52eb9b2). The shaper enters the loop-broken sub-model as nothing more than the tail gate node (CLKT) held at the same steady VDD evaluate bias; the sub-model therefore measures the identical structure, and this record state-checks the no-regression acceptance criterion against the ratified <= 1.0 mV / <= 0.6 mV rms bounds.
- **Overall**: measured value recorded (informational plumbing proof, not pass/fail -- see Claim)

## Measured value(s)

| Quantity | Value | Corner condition |
|---|---|---|
| Op point: v(TAIL) | 0.0294 V | tt/27.0C/1.8V |
| Op point: v(DIP)=v(DIN) | 0.2418 V | tt/27.0C/1.8V |
| Single-ended input-referred noise (`inoise_total`, referred through Vinp) | 0.3158 mV rms | tt/27.0C/1.8V |
| **Differential input-referred noise estimate** (`sqrt(2) x` single-ended, diff-pair noise-doubling approximation) | **0.4466 mV rms** | tt/27.0C/1.8V |

- **Data provenance**: model-card-monte-carlo (sky130A BSIM4 device noise models via ngspice's `.noise` analysis; no literature/foundry-doc noise figure used)
- **Ratified bound comparison (DR-002)**: the Input-referred noise row is RATIFIED at <= 1 mV rms differential (target) / <= 0.6 mV rms (stretch). This record's 0.4466 mV rms differential estimate (a LOWER BOUND, see Methodology) clears the target bound and clears the stretch bound.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `bb328505e5754095d5f4682c0d2ac84d593505eb` on `feature/issue-30` (dirty)
- DUT netlist sha256: `f337a354d61fafba0262acd33c8bdba31ed578815140fb2cf1228963fa52d5d8`

- **Supersedes**: (none)

Written by `sim/comparator-decision/run.py noise`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).
