# Monte Carlo record 20260916-003531-52eb9b2

- **Record ID**: 20260916-003531-52eb9b2
- **Claim**: None -- the top-level README's target-spec table is DRAFT and unratified (spec/README.md), so no row exists here to substantiate or fail. What this record DOES characterize, unlike every record written before issue #24, is **this repo's own comparator**: `design/comparator.sch`, the DR-001 topology at a sizing derived from this PDK's own mismatch models (see that schematic's sizing-rationale block and DR-001 Amendment 1). The DRAFT table's rows are quoted in the analysis below for orientation only -- as the design intent this sizing pass aimed at, never as a pass/fail grade against a ratified bound.
- **Netlist provenance**: schematic-derived (`design/comparator.sch` -> `./design/netlist.sh` -> `sim/comparator-decision/testbench/comparator_core.spice`)
- **Statistical convention**: mismatch corner `tt_mm`, N=16, seed=1 (draws use seed, seed+1, ..., seed+N-1), PVT point process=tt temp=27.0C supply=1.8V. Relative standard error on the estimated offset stdev, SE(s)/s ~= 1/sqrt(2(N-1)): N=16 gives 18.3% -- a distribution-shape-adequate sample for this plumbing proof, not a sample sized for a tight yield-fraction claim (that needs O(100s)).
- **Methodology**: linearized pick-off statistic at t=evaluate_start+0.3ns, calibrated to an input-referred gain of 5.8156 V/V via a 4-point ideal-device Vindiff sweep (zero-intercept least-squares fit).
- **Negative control**: N=16 draws at the plain `tt` corner (mismatch DISABLED), same seed sequence -- PASS (stdev == 0 on the pick-off-derived offset)
- **Positive control**: N=16 draws at the `tt_mm` corner (mismatch ENABLED) offset stdev=0.00201899 V (> 0, shows genuine spread -- PASS)
- **Overall**: PASS

## Gain calibration (ideal devices, tt corner)

| Vindiff (mV) | pick-off diff (V) |
|---|---|
| 1.00 | 0.00582013 |
| 2.00 | 0.0116399 |
| 5.00 | 0.0290939 |
| 10.00 | 0.0581459 |

Fitted gain (zero-intercept least squares): **5.8156 V/V**

## Offset distribution (mismatch-enabled draws, input-referred)

| N | mean (mV) | stdev (mV) | min (mV) | max (mV) |
|---|---|---|---|---|
| 16 | -0.5711 | 2.0190 | -3.2711 | 3.3168 |

## Negative control (mismatch-disabled, same seed sequence)

| N | mean (mV) | stdev (mV, must be 0) |
|---|---|---|
| 16 | 0.0000 | 0 |

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `52eb9b2571e2ba8376d1103d2014af26e231cad9` on `feature/issue-24` (dirty)
- DUT netlist sha256: `8613e8f90be1c4d478e21181df390754234bd88123db03b3e939a384f6feec88`
- MC seed: 1
- MC N: 16

- **Supersedes**: (none)

Written by `sim/comparator-decision/run.py offset`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).
