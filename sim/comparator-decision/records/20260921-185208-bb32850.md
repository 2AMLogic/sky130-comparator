# Record 20260921-185208-bb32850

- **Record ID**: 20260921-185208-bb32850
- **Claim**: None by itself -- top-level README target-spec rows are graded by their ratifying decision record, and per DR-002 (2026-09-16, merged) the Offset sigma, Input-referred noise, and Kickback rows are RATIFIED while Decision time vs. overdrive and Supply/power stay DRAFT/OPEN. What this record characterizes is **this repo's own comparator**: `design/comparator.sch`, the DR-001 topology plus the DR-003 soft-clock shaper on the clock port, at a sizing derived from this PDK's own mismatch models (see the schematic's sizing-rationale block, DR-001 Amendment 1, and DR-003). Any statement about a ratified row's compliance made below cites the bound and the number side by side.
- **Netlist provenance**: schematic-derived (`design/comparator.sch` -> `./design/netlist.sh` -> `sim/comparator-decision/testbench/comparator_core.spice`)
- **Corner matrix run**: process=['tt'], temperature_c=[27.0], supply_v=[1.8] (1 PVT point, both variants -- **subset-corner justification**: this record re-measures the SAME single nominal corner (tt/27C, 1 kOhm, 50 mV overdrive) issue #26's kickback record (20260916-060139-f1eb978, the pre-mitigation measurement DR-002's Kickback disposition cites) used, so the before/after comparison DR-002 asked for is direct and like-for-like per issue #30's acceptance criteria -- a full-corner kickback sweep remains open work exactly as DR-002 already flags it)
- **Stimulus**: VINP/VINN biased at VCM=0.9V +/- 25mV (a static 50mV differential step, sized to guarantee a clean decision -- matching the `regen` sweep's largest tested overdrive and the target-spec table's own "Decision time vs. overdrive" 50mV reference point); single reset(5.0ns, CLK=0)->evaluate(CLK=1.8V) edge, the same stimulus shape `regen`/`reset` already use
- **Variants**: `loaded` = VINP/VINN fed through an explicit 1000ohm series resistor from an ideal DC source -- the target-spec Kickback row's own stated methodology assumption, and what lets an injected disturbance show up as a voltage instead of being absorbed. `ideal` = CONTROL, VINP/VINN driven directly by the ideal DC source (zero source impedance) -- must collapse to (numerically) zero by construction; anything else would mean the deck is not actually isolating the source-impedance-dependent effect the `loaded` variant measures.
- **Sensitivity criteria**: `loaded` peak disturbance must be >= 1mV (a floor far below the measured signal, not a tuned threshold); `ideal` peak disturbance must be <= 1e-06mV
- **Note**: Chain-corrects the claim-preamble wording of 20260921-185034-bb32850 and its immediate re-run 20260921-185118-bb32850, which both described the mitigation as an input isolation chain -- a first design iteration this PR measured and REJECTED (regressing the ratified noise row); the measurement itself is unchanged in all three records. Post-mitigation re-measurement after the DR-003 soft-clock shaper (issue #30's mitigation; spec/decision-records/DR-003-kickback-slew-limited-clock.md): R_CLKS + M_CLKCAP (~250 ps) now drive all five clocked gates from CLKT, stretching the evaluate onset. Same tt/27C, 1 kOhm, 50 mV overdrive corner as record 20260916-060139-f1eb978 (144.60 mV peak pre-mitigation -- DR-002's Kickback-row citation) for the direct before/after comparison issue #30's acceptance criteria ask for.
- **Overall**: PASS (loaded peak=85.7071mV, ideal peak=0.0000mV)

## Peak input-node disturbance

| Variant | peak \|VINP-quiescent\| (mV) | peak \|VINN-quiescent\| (mV) | Result |
|---|---|---|---|
| loaded | 85.7071 | 75.6747 | peak=85.7071mV -> as expected |
| ideal | 0.0000 | 0.0000 | peak=0.0000mV -> as expected |

## Reading this record

Measured against the DR-003 soft-clock shaper (design/comparator.sch after issue #30's mitigation: the poly resistor R_CLKS and MOS capacitor M_CLKCAP form the internal node CLKT, tau ~ 250 ps, and CLKT -- not CLK -- drives all five clocked gates, so the whole evaluate onset is slew-limited): the raw kickback mechanism -- the input-pair devices' own gate-drain parasitic capacitance coupling the internal precharged nodes' (DIP/DIN) fast swing at the reset->evaluate transition back onto the VINP/VINN gate nodes -- is unchanged in kind, but its SOURCE amplitude now scales with the shaped (slower) DIP/DIN collapse rate instead of the testbench's 100 ps ramp transient. The `loaded` figure below includes both the positive (tail-switch gate-capacitance) and negative (DIP/DIN collapse) lobes the same way the pre-mitigation record did. The `ideal` control still collapses to (numerically) zero by construction, so the deck continues to isolate a genuine source-impedance-dependent effect.

- **Ratified bound comparison (DR-002)**: the Kickback row is RATIFIED at <= 5 mV disturbance into 1 kOhm source impedance (target, single decision edge) / <= 2 mV (stretch). This record's `loaded` peak figure of 85.7071 mV DOES NOT yet meet the target bound -- the DR-002 non-compliance finding stands; this figure is honest partial-improvement information for the decision records to weigh, never silently superseding DR-002's disposition. The comparison holds for the single tt/27C PVT point this record runs (see Corner matrix run above); kickback PVT coverage remains open work per DR-002.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `bb328505e5754095d5f4682c0d2ac84d593505eb` on `feature/issue-30` (dirty)
- DUT netlist sha256: `8953c209f6bf911f4c2f578302fdb62db4c754db88a148f57e3d5ca1c44cc761`

- **Supersedes**: 20260921-185118-bb32850

Written by `sim/comparator-decision/run.py kickback`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).
