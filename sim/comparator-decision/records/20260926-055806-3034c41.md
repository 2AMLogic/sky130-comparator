# Record 20260926-055806-3034c41

- **Record ID**: 20260926-055806-3034c41
- **Claim**: None by itself -- top-level README target-spec rows are graded by their ratifying decision record, and per DR-002 (2026-09-16, merged) the Offset sigma, Input-referred noise, and Kickback rows are RATIFIED while per DR-005 (2026-09-22, this campaign's record) Decision time vs. overdrive is RATIFIED and only Supply/power stays DRAFT/OPEN. What this record characterizes is **this repo's own comparator**: `design/comparator.sch`, the DR-004 static-preamplifier + StrongARM-latch topology (superseding DR-001's no-preamp scoping) with the DR-003 soft-clock shaper still on the clock port, at a sizing derived from this PDK's own mismatch models (see the schematic's sizing-rationale block, DR-001 Amendment 1, DR-003, DR-004, and DR-005). Any statement about a ratified row's compliance made below cites the bound and the number side by side.
- **Netlist provenance**: POST-LAYOUT / EXTRACTED (`layout/comparator.gds` -> `klt extract --parasitics` -> `layout/comparator.extract.spice` -> `layout/extract_pex.py` -> `layout/comparator.pex.spice`), extracted by klt 0.6.0 on klayout 0.30.10 -- the pin `docs/environment-setup.md` records and CI installs, asserted by `layout/extract_pex.py` because extracted resistances are not stable across builds. Lumped-RC parasitics: 69 star resistors, 12 net-to-ground capacitors and 21 net-to-net coupling capacitors, 117.65 fF total ground capacitance and 17.52 kohm total series resistance over 12 nets (`layout/extract-parasitics.json`). The per-net series R is the extractor's DEFAULT single-lumped-star model, which is coarse on the supply nets specifically (GND and VDD carry the bulk of it) and is expected to be pessimistic there -- see `sim/comparator-decision/README.md`. Net names are the SCHEMATIC names, carried across by the 12/12 net correspondence in the committed `layout/lvs-report.json`, so every probe in this deck names the same circuit node the schematic-level record's probe named. This fragment deliberately carries the `res_high_po` w=0.42 um drawn vs. w=0.35 um schematic delta disclosed by PR #55 / `layout/lvs-coverage-probe.json` -- it is what the layout actually draws, so it is what a post-layout measurement must see.
- **Corner matrix run**: process=['fs'], temperature_c=[125.0], supply_v=[1.8] (1 point)
- **Noise methodology**: `tran-noise-mc` (issue #41), REGENERATION-INCLUSIVE by equivalent-source injection -- per-side TRNOISE sources at the comparator inputs (preamp input-referred rms from the AC `noise` sub-command: 0.6663 mV) and in series with the steering gates (latch front-end gate-referred rms from a new steering+tail AC sub-model: 0.3875 mV, load-cap insensitivity verified), propagated through the FULL committed fragment's real clocked evaluate trajectory. Injected rms calibrated empirically against this ngspice build's TRNOISE semantics (std = 0.8599 x na; achieved 0.6715 mV input / 0.3878 mV gate vs the AC targets), correlation time 3.7e-10s (~the 1GHz AC band's Nyquist interval). Stated residual omissions: the cross-coupled PMOS pair's noise during exponential separation (divided by the growing regenerative gain) and the reset PMOS (off in evaluate).
- **Two statistics, one claim**: (a) pick-off MC at Vindiff=0, N=32 seeds, gain 17.5364 V/V -> input-referred sigma **0.2540 mV** (95% CI [0.1961, 0.3009] mV). (b) The decision-transition cross-check is NOT MEASURABLE at this corner: every pair was degenerate, and the reason is read off the counts in the table below rather than asserted from a canned string -- every one of the 16 runs RESOLVED (0 unresolved) and every one of them decided the SAME way -- negative -- at BOTH signs of the overdrive, so p+ = p- exactly and the pairs carry no sigma information. That is not a noise statement and must not be read as one: it says a DETERMINISTIC term larger than these sigma-scaled overdrives (tens to hundreds of uV) is setting the outcome. Compare the systematic input offset the `offset` sub-command measures at this same corner and provenance before reading anything else into it. Either way the consequence for THIS record is the same: at this corner the decision statistics are not noise-bound, so the pick-off figure stands alone and the cross-check is deferred to the corners where it is measurable.
- **How the injection is made post-layout** (issue #65): on the extracted netlist each steering device's gate sits on its own parasitic star leg (`OUTP1__t4`, `OUTN1__t4`) tied to the preamp-output hub through that terminal's share of the net's series resistance (75.87 / 97.49 ohm), so there is no node on which to insert a series source by naming the gate net. The deck therefore RE-POINTS those two star-leg resistors from `OUTP1`/`OUTN1` onto the injected nodes `GST_P`/`GST_N` (values unchanged) and places the sources `Vstp GST_P OUTP1` / `Vstn GST_N OUTN1` exactly as the schematic-level deck does; the device lines and every other extracted element are byte-identical to the committed fragment, so the whole injection is two changed tokens. Inserting on this side of the leg rather than between the gate terminal and its leg is not a judgement call: a star-leg node carries exactly one device terminal and exactly one star resistor (asserted by the deck builder, not assumed), so the leg resistor and an ideal source are two-terminal elements in series and the two placements are the same network. Convention for split devices (a schematic device the layout draws as several parallel fingers): all fingers are driven from the one injected node, each keeping its own re-pointed leg. It is not exercised here -- the steering pair is drawn as ONE W=8 device each, and the split devices on this layout are the W=13 input pair, whose injection is at the comparator PINS and needs no surgery at all.
- **Where the stage-2 AC anchor comes from post-layout**: the latch front-end gate-referred rms is measured on the committed LATCH FRONT-END PARTITION of the extracted netlist (`layout/comparator.pex-latch.spice` -- steering pair + latch tail with their extracted parasitics, selected by connectivity as `every device on the TAIL2 node`), the exact counterpart of the schematic side's three named device lines, and the counterpart of the preamp partition the AC `noise` sub-command already used post-layout.
- **Note**: Post-layout (extracted) regeneration-inclusive noise at fs/125C -- issue #83, and this is DR-006's OWN NAMED CLOSURE CONDITION ("a regeneration-inclusive input-referred noise measurement against the extracted netlist at fs/125C"), the corner that binds the Input-referred noise row. SAMPLE SIZE IS SMALLER THAN THE tt/27C POST-LAYOUT COUNTERPART AND IS STATED ON THE FACE OF THIS RECORD rather than left to be inferred: N=32 pick-off seeds and 4 seeds/sign/decision point, against record 20260925-214740-81f594b's N=64 and 16 and the schematic-level records' N=128 and 64. The reason is wall clock under a MEASURED, PREVIOUSLY UNRECORDED host constraint, not method: this dispatch host budgets each agent a ONE-CORE cgroup CPU quota (cpu.max 100000 100000 on the agent's systemd scope), so --jobs 2 buys no parallel throughput here and the run is quota-bound, one deck at a time; and the extracted decks at THIS corner measured ~350-500 s of CPU each, roughly 3x the tt/27C cost the issue-#65 budget was extrapolated from (fs skew at 125C is the stiffest graded corner for this fragment). At that cost the counterpart's (64 + 64) decks is a ~10 h quota-bound run and (128 + 256) is over a day; this configuration is 52 decks and completed in 64 min once co-tenant load fell. Nothing else about the methodology changes -- same sub-command, same deck template, same methodology constants, run through the identical run_noise_tran(...) the CLI invokes for `noise-tran --dut extracted --corner fs --temp 125 --n 32 --seeds-per-point 4 --jobs 1`. HOW THE SMALLER N IS HANDLED: it is handled by reading the disposition below against the CONSERVATIVE END OF THE 95% CI, never the point estimate. THE DR-006 READING, STATED EXPLICITLY BECAUSE THAT IS WHAT THIS RECORD EXISTS FOR: the measured regeneration-inclusive figure is 0.2540 mV rms differential (95% CI [0.1961, 0.3009]), which clears the RATIFIED <= 1.0 mV target bound by 3.94x -- 3.32x at the CI's upper bound. It ALSO clears the <= 0.6 mV stretch figure (2.36x; 1.99x at the CI's upper bound), but see the caveat below before reading that as a stretch claim. A deliberately PESSIMISTIC cross-check, stated so the closure does not rest on a single arithmetic: even if the entire measured regeneration-inclusive figure were (incorrectly) treated as an INDEPENDENT term to be added in quadrature on top of this corner's AC lower bound of 0.9423 mV rms -- which DOUBLE-COUNTS, because both figures are dominated by the SAME preamp input-referred source (0.6663 mV rms/side here) and are two estimates of one quantity rather than two separate terms -- the combination is sqrt(0.9423^2 + 0.2540^2) = 0.9759 mV rms, STILL INSIDE the <= 1.0 mV target (1.025x; 0.9892 mV / 1.011x using the CI's upper bound). So the target bound holds under both the correct direct reading and the indefensibly-conservative double-counting one, and DR-006's 0.335 mV rms quadrature allowance (sqrt(1.0^2 - 0.9423^2)) is not exceeded by the measured figure either (0.2540 mV, 0.76x of it). WHAT DOES NOT CLOSE, recorded so this is not read as more than it is: (i) the AC loop-broken lower bound at this corner is still 0.9423 mV rms with only 1.06x target margin, and any statement that cites THAT figure as the row's basis still carries that margin -- what changes is that the AC figure is no longer the row's only basis at this corner; (ii) the <= 0.6 mV stretch figure stays recorded as BREACHED at four of seven corners on the AC basis (DR-006 Decision 4) and nothing here relaxes that -- the two bases disagree on stretch, the transient basis has only 2 of 7 corners, and per CLAUDE.md the breach record stands rather than being erased by a different method's number; (iii) the remaining five graded corners are still unmeasured post-layout for this sub-command. METHOD CONSISTENCY, as corroboration: the AC-to-transient gap at this corner is 3.71x (0.9423 / 0.2540), squarely inside the ~3-4x gap DR-006 itself recorded at tt/27C, ss/-40C and ff/125C (tt/27C post-layout is 4.54x), so this corner's transient figure is not an outlier of the method. NEW FINDING: the regeneration-inclusive figure is RESOLVABLY worse at fs/125C than at tt/27C -- 0.2540 mV against 0.1448 mV, 1.754x, and the two 95% CIs ([0.1961, 0.3009] here against [0.1224, 0.1641] there) DO NOT OVERLAP, so unlike that record's schematic-to-layout ratio this corner-to-corner difference is resolved at these sample sizes. It degrades faster with corner than the AC sub-model's own 1.433x (0.9423 / 0.6576), which is the same "the hot corners bind this row" shape DR-006's consequences recorded, now confirmed on the regeneration-inclusive basis rather than inferred from the lower bound. NO COMMITTED SCHEMATIC-LEVEL noise-tran RECORD EXISTS AT THIS CORNER (the three that exist are tt/27C, ss/-40C and ff/125C), so there is no ratio to report and the Post-layout delta section says exactly that instead of inventing one -- the same convention the post-layout `noise` records use at their six counterpart-less corners.
- **Overall**: MEASURED (informational characterization, not pass/fail -- see Claim)

## Injection calibration

| Injected term | AC target (mV rms/side) | achieved (mV rms/side) |
|---|---|---|
| preamp, at comparator inputs | 0.6663 | 0.6715 |
| latch front-end, in series with steering gates | 0.3875 | 0.3878 |

## AC anchors (this corner)

| Quantity | Value |
|---|---|
| Preamp single-ended input-referred (AC `noise` sub-command, 1kHz-1GHz) | 0.6663 mV rms |
| Latch front-end gate-referred (steering+tail sub-model, same band, gate CM 1.3888V, op TAIL2 0.2888V) | 0.3875 mV rms |
| Pick-off gain (ideal-device calibration) | 17.5364 V/V |

## Pick-off Monte Carlo (statistic (a))

| N | sigma input-referred (mV) | 95% CI (mV) |
|---|---|---|
| 32 | 0.2540 | [0.1961, 0.3009] |

## Decision-transition cross-check (statistic (b))

| \|Vod\| (mV) | k (x sigma_hat) | +Vod: ones/M | -Vod: ones/M | unresolved | pair sigma (mV) |
|---|---|---|---|---|---|
| 0.1905 | 0.75 | 0/4 | 0/4 | 0 | degenerate |
| 0.3811 | 1.5 | 0/4 | 0/4 | 0 | degenerate |

Inverse-variance-weighted decision sigma: **not measurable at this corner** (every pair degenerate -- the reason is derived from these counts in the **Two statistics** bullet above, and is not restated here so the two cannot drift apart)

- **Ratified bound comparison (DR-002)**: the Input-referred noise row is RATIFIED at <= 1 mV rms differential (target) / <= 0.6 mV (stretch). This record's regeneration-inclusive pick-off sigma 0.2540 mV (differential; the per-side injections are independent, the same convention the sqrt(2) AC estimate uses) clears the target bound and clears the stretch bound.

## Post-layout delta vs. the schematic-level record

No committed schematic-level `noise-tran` record exists at fs/125.0C to difference this figure against, so this record states the post-layout number alone. It is not a delta.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `3034c41bd5514c854c98216fb21983d1309f68a3` on `feature/issue-83` (dirty)
- DUT netlist sha256: `918aa6ca89d06ef75c684dd216feed017a3ae1cfdff185d95bac54b2741833d0`
- noise-tran N pickoff: 32
- noise-tran seeds/sign/decision point: 4

- **Supersedes**: (none)

Written by `sim/comparator-decision/run.py noise-tran`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).
