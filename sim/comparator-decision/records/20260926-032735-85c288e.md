# Monte Carlo record 20260926-032735-85c288e

- **Record ID**: 20260926-032735-85c288e
- **Claim**: None by itself -- top-level README target-spec rows are graded by their ratifying decision record, and per DR-002 (2026-09-16, merged) the Offset sigma, Input-referred noise, and Kickback rows are RATIFIED while per DR-005 (2026-09-22, this campaign's record) Decision time vs. overdrive is RATIFIED and only Supply/power stays DRAFT/OPEN. What this record characterizes is **this repo's own comparator**: `design/comparator.sch`, the DR-004 static-preamplifier + StrongARM-latch topology (superseding DR-001's no-preamp scoping) with the DR-003 soft-clock shaper still on the clock port, at a sizing derived from this PDK's own mismatch models (see the schematic's sizing-rationale block, DR-001 Amendment 1, DR-003, DR-004, and DR-005). Any statement about a ratified row's compliance made below cites the bound and the number side by side.
- **Netlist provenance**: POST-LAYOUT / EXTRACTED (`layout/comparator.gds` -> `klt extract --parasitics` -> `layout/comparator.extract.spice` -> `layout/extract_pex.py` -> `layout/comparator.pex.spice`), extracted by klt 0.6.0 on klayout 0.30.10 -- the pin `docs/environment-setup.md` records and CI installs, asserted by `layout/extract_pex.py` because extracted resistances are not stable across builds. Lumped-RC parasitics: 69 star resistors, 12 net-to-ground capacitors and 21 net-to-net coupling capacitors, 117.65 fF total ground capacitance and 17.52 kohm total series resistance over 12 nets (`layout/extract-parasitics.json`). The per-net series R is the extractor's DEFAULT single-lumped-star model, which is coarse on the supply nets specifically (GND and VDD carry the bulk of it) and is expected to be pessimistic there -- see `sim/comparator-decision/README.md`. Net names are the SCHEMATIC names, carried across by the 12/12 net correspondence in the committed `layout/lvs-report.json`, so every probe in this deck names the same circuit node the schematic-level record's probe named. This fragment deliberately carries the `res_high_po` w=0.42 um drawn vs. w=0.35 um schematic delta disclosed by PR #55 / `layout/lvs-coverage-probe.json` -- it is what the layout actually draws, so it is what a post-layout measurement must see.
- **Statistical convention**: mismatch corner `ff_mm`, N=16, seed=1 (draws use seed, seed+1, ..., seed+N-1), PVT point process=ff temp=27.0C supply=1.8V. Relative standard error on the estimated offset stdev, SE(s)/s ~= 1/sqrt(2(N-1)): N=16 gives 18.3%. A distribution-shape-adequate sample for this plumbing proof, not a sample sized for a tight yield-fraction claim (that needs O(100s)).
- **Methodology**: linearized pick-off statistic at t=evaluate_start+0.65ns, calibrated to an input-referred gain of 35.7356 V/V via a 4-point ideal-device Vindiff sweep (zero-intercept least-squares fit).
- **Negative control**: N=16 draws at the plain `ff` corner (mismatch DISABLED), same seed sequence -- PASS (stdev == 0 on the pick-off-derived offset)
- **Positive control**: N=16 draws at the `ff_mm` corner (mismatch ENABLED) offset stdev=0.00247959 V (> 0, shows genuine spread -- PASS)
- **Note**: Post-layout (extracted) offset at `ff_mm`/27C -- issue #80, closing the four `_mm` mismatch corners issue #64's post-layout PVT campaign deliberately skipped. Same methodology anchor every committed offset record uses (N=16, seed 1, pick-off 0.65 ns, 27 C), so this differs from its schematic-level counterpart 20260922-174326-e23c509 only in the DUT fragment. `ff_mm` was the LOWEST schematic-level corner (1.5954 mV); #64 established that the post-layout penalty is not a corner-independent constant, so whether it stays lowest is measured, not assumed. The mismatch-disabled negative control's MEAN is the layout-induced SYSTEMATIC offset term -- 0.6547 mV at `tt_mm` (20260925-112809-4694692) -- and tracking how it moves across corners is a second output of this work (relates to #66).
- **Overall**: PASS

## Gain calibration (ideal devices, ff corner)

| Vindiff (mV) | pick-off diff (V) |
|---|---|
| 1.00 | 0.0556644 |
| 2.00 | 0.0885934 |
| 5.00 | 0.186941 |
| 10.00 | 0.347807 |

Fitted gain (zero-intercept least squares): **35.7356 V/V**

## Offset distribution (mismatch-enabled draws, input-referred)

| N | mean (mV) | stdev (mV) | min (mV) | max (mV) |
|---|---|---|---|---|
| 16 | 0.1634 | 2.4796 | -3.8123 | 6.2408 |

- **Ratified bound comparison (DR-002)**: the Offset sigma row is RATIFIED at <= 15 mV, 3-sigma (target) / <= 8 mV, 3-sigma (stretch), input-referred. This record's N=16 estimated stdev 2.4796 mV gives 3-sigma = 7.4388 mV, which clears the target bound and clears the stretch bound. N=16 is sized for distribution shape, not a yield-fraction claim (see the Statistical convention above).

## Negative control (mismatch-disabled, same seed sequence)

| N | mean (mV) | stdev (mV, must be 0) |
|---|---|---|
| 16 | 0.6355 | 0 |


## Post-layout delta vs. the schematic-level record

| Quantity | Schematic-level | Post-layout | Delta | Ratio |
|---|---|---|---|---|
| input-referred offset stdev | 1.5954 mV | 2.4796 mV | +0.8842 mV | 1.554x |

- **Schematic-level counterpart record**: `sim/comparator-decision/records/20260922-174326-e23c509.md` (N=16 draws at `ff_mm`, seed 1, pick-off 0.65 ns, same-seed negative control stdev exactly 0)
- **What differs between the two runs**: the DUT fragment, and nothing else. Same sub-command, same corner/temperature/supply, same methodology constants, same deck template -- the schematic-side deck text is byte-identical to the one that produced the counterpart record.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `85c288e380dbc49969169210388f2fb06a78e64b` on `feature/issue-80` (dirty)
- DUT netlist sha256: `7b83ea8cf54e90f312be9ff7d7803eeae41a20a689a721928e5acf4703ed7c0e`
- MC seed: 1
- MC N: 16

- **Supersedes**: (none)

Written by `sim/comparator-decision/run.py offset`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).
